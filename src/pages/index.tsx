import { Page } from "zmp-ui";

function HomePage() {
  return (
    <Page style={{ paddingTop: 'var(--safe-top)', paddingBottom: 'var(--safe-bottom)' }}>
    </Page>
  );
}

export default HomePage;
